<%= form_with model: @clienttype, local: true do |form| %>
 
  <% if @clienttype.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@clienttype.errors.count, "error") %> prohibited
        this clienttype from being saved:
      </h2>
      <ul>
        <% @clienttype.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :text %><br>
    <%= form.text_field :text, class: "form-control" %>
  </p>
 
  <p>
    <%= form.submit class: "btn btn-default" %>
  </p>
 
<% end %>